<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PROJECT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practice">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bottle.bottle" />
        <activity android:name=".brick.arkanoid_main" />
        <activity android:name=".brick.arkanoid_game" />
        <activity android:name=".snake.snakegame" />
        <activity android:name=".snake.snake" />
        <activity android:name=".memory.Memory"/>
    </application>

</manifest>